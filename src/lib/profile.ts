import{supabase}from'./supabase';export type AppRole='admin'|'staff'|'viewer';export type Profile={id:string;full_name:string;role:AppRole;is_active?:boolean;active?:boolean;created_at?:string;updated_at?:string;username?:string|null;email?:string|null;archived_at?:string|null;employee_code?:string|null;department?:string|null};export async function getMyProfile(){const{data:{user}}=await supabase.auth.getUser();if(!user)return null;const{data,error}=await supabase.from('profiles').select('*').eq('id',user.id).maybeSingle();if(error)return null;return data as Profile|null}
