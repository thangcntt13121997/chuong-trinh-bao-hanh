import{useEffect,useState}from'react';import{api}from'../lib/api';import{getMyProfile,type Profile}from'../lib/profile'
export default function AuditLogs(){const[me,setMe]=useState<Profile|null>(null),[rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState('');useEffect(()=>{void(async()=>{const p=await getMyProfile();setMe(p);if(p?.role==='admin')try{const d=await api<any>({action:'list_audit'});setRows(d.rows)}catch(e:any){setMsg(e.message)}})()},[]);if(me&&me.role!=='admin')return <div className="card"><h2>Không có quyền truy cập</h2></div>;return <><div className="page-head"><div><h1>Nhật ký thay đổi</h1><p>Ghi nhận thay đổi sản phẩm, hồ sơ xử lý, file và nhân viên.</p></div></div>{msg&&<div className="alert danger">{msg}</div>}<div className="card table-wrap"><table><thead><tr><th>Thời gian</th><th>Người thao tác</th><th>Đối tượng</th><th>Hành động</th><th>Chi tiết</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString('vi-VN')}</td><td>{r.actor_name||r.actor_id||'—'}</td><td>{r.entity_type}<small>{r.entity_id}</small></td><td>{r.action}</td><td><details><summary>Xem</summary><pre>{JSON.stringify({before:r.before_data,after:r.after_data},null,2)}</pre></details></td></tr>)}</tbody></table></div></>}
